# holdem-pub-iso 작업 메모

새 세션을 시작하면 아래 "진행 상황 로그"부터 읽고 이어서 작업할 것.
세션을 마칠 때는 맨 위에 날짜와 함께 새 항목을 추가한다 (무엇을 했는지 / 다음에 할 일).

---

## 이 저장소가 무엇인가

**아이소메트릭 홀덤펍 경영 게임.** `holdem-pub-tycoon`에서 아이소 렌더러와 에셋
파이프라인만 가져와 새로 시작한 저장소다. **게임 로직은 같고 디자인(아트 방향)이
크게 달라진다** — 그래서 렌더러/파이프라인은 그대로 두고 `assets/pack/<테마>/`
폴더만 갈아 끼우는 구조를 유지한다.

빌드 도구 없음. `index.html`을 그대로 열면 돌아간다 (GitHub Pages 배포).

### 가져온 것 / 버린 것

| 가져옴 | 버림 |
|---|---|
| `js/pub-scene-2d.js` — 아이소 2D 픽셀 렌더러 | `js/scene3d.js` — Three.js 3D 렌더러 (죽은 코드) |
| `assets/pack/` — 테마 5종 × (기물 60 + 인물 38) | `assets/pixel/`, `assets/actors/` — 구 파이프라인 산출물 |
| `tools/pack-spec · pack-build · sheet-cut · _canvas` | `tools/actor-*`, `sheet-slice`, `*-preview` (구 파이프라인) |
| `tools/scene2d-preview.mjs` — 브라우저 없이 한 프레임 검증 | `game-pixel.html`, `slice-*.html` 등 스크래치 페이지 |
| `js/game.js` · `data.js` — 경제/업그레이드/딜러 로직 | `index-2d.html` (index.html과 중복) |

과거 로그 전문은 `docs/legacy-log.md`에 그대로 두었다. 아래 "절대 다시 밟지 말 것"은
그 로그에서 값을 치르고 얻은 규칙만 추린 것이다.

---

## 절대 다시 밟지 말 것 (이관된 규칙)

### 에셋 크기
- **크기는 미터로 적는다.** `tools/pack-spec.mjs`의 `SIZE`에 `[가로 m, 세로 m, 높이 m]`.
  타일 1칸 = 1m, 사람 1.7m = 28px, 1m ≈ 16.5px. "긴 변 몇 px"이나 눈대중 높이로
  주면 문이 사람 두 배가 되고 의자가 사람보다 커진다.
- **사람은 시트 한 장에서 배율을 한 번만 구해 24칸 전부에 적용한다.** 칸마다 자기
  바운딩 박스를 28px에 맞추면 팔 든 그림은 작아지고 앉은 그림은 커진다.
- 앉은 칸은 높이 상한(`ACTOR.sit`)을 반드시 건다. 2등신은 앉아도 박스가 안 줄어든다.

### 시트 생성
- **낱개로 뽑지 말고 한 장에 12칸씩 뽑는다.** 낱개는 시점·광원·외곽선·팔레트가
  조금씩 달라져 한 화면에 모으면 따로 논다.
- **개수와 열 수를 문장 맨 앞에 STRICT로 못박는다.** 안 그러면 12개 요구에 6열 18개를
  그려 오고, 절반 해상도로 나와 조각이 붙는다.
- **머리카락을 명시한다.** 2등신 비율만 강조하면 전원 대머리로 나온다.
- **뒷모습은 별도 시트로.** 머리와 옷이 같은 색이면 28px에서 갈색 덩어리가 된다 —
  머리를 옷보다 밝게, 목덜미 선, 어깨 각을 명시.
- **한 칸에 물건 하나.** 테이블 칸에 의자·방석·칩을 같이 그리게 두면 배치 단계에서
  의자가 중복된다.
- 배경은 마젠타(키잉용), 3톤, 어두운 외곽선, 좌상단 광원으로 통일.
- 간판 글씨는 생성에 맡기지 않는다 — `PixelFont`로 코드에서 찍는다.
- 테마 모티브는 분위기만 쓴다. 작품명·스튜디오·캐릭터 이름을 프롬프트에 넣지 않는다.

### 렌더링
- 아이소 방은 화면에서 2:1이라 **방을 화면보다 넓게 잡고 좌우 꼭짓점을 화면 밖으로
  흘려야** 커 보인다. 다 넣으면 절대 커 보이지 않는다.
- 배치는 **화면 좌표에서 역산**한다: `diff=(px−OX)/(TW/2)`, `sum=(py−OY)/(TH/2)`.
  격자에 고르게 놓으면 화면에서는 겹친다.
- 좌석은 격자 원이 아니라 **화면 좌표 타원**. 격자 원은 화면에서 대각선으로 늘어난다.
- 바닥 접지는 `blitProp(key, cx, cy)` 하나로 — 물건별 보정값을 주지 않는다.
- 벽 가구 방향은 `manifest.json`의 그림 축(gx/gy/대칭)을 보고 **자동 반전**한다.
- 앞벽이 없으면 "건물 안"으로 안 보인다. 벽 색을 바닥과 같게 두면 벽이 없어 보인다.

### 작업 습관
- **크레딧 쓰기 전에 코드로 되는지 먼저 본다.** 크기 불일치·방향 문제는 대부분
  생성이 아니라 굽는 단계와 렌더러의 버그였다.
- 고칠 때마다 `node tools/scene2d-preview.mjs`로 한 프레임 찍어 확인한다.
  에셋 로드·탭 판정·배치가 한 번에 걸러진다.
- 동시 생성은 6장쯤에서 429가 난다. 나눠 던진다.

---

## 에셋 파이프라인

```
assets/raw/sheets/*.png      ← 생성 원본 (gitignore, 로컬에만)
        │  node tools/pack-build.mjs [테마]
        ▼
assets/pack/<테마>/props/*.png + actors/*.png + manifest.json   ← 커밋 대상
        │  js/pub-scene-2d.js 가 THEME_SET 으로 폴더만 바꿔 읽음
        ▼
                    화면
```

- 크기 기준표: `tools/pack-spec.mjs` (이 파일 하나가 모든 크기를 정한다)
- 굽기: `tools/pack-build.mjs` — 마젠타 키잉 → 격자선 제거 → 연결 성분 → 크기 통일
- 검증: `node tools/scene2d-preview.mjs [테이블수] [점유율]` → `tools/_preview/scene2d.png`
- 현재 테마: `classic`, `japanese`, `kabukicho`, `neon`, `european`, `princess`

### 생성 비용 (2026-09-19 기준)
- Nano Banana 2 — **2 크레딧/장** (지금 pack 시트를 뽑은 모델)
- z_image — **0.15 크레딧/장** (썸네일·아이콘용)
- 한 테마 = 시트 8장(기물 4 + cast/back/staffmo/seat) = 16 크레딧, 재시도 포함 20~35

---

## 진행 상황 로그

### 2026-09-19 (2차) — 새 테마 `kabukicho` + 마젠타 키잉 재설계 (생성 10회, 20크레딧)

- **새 아트 방향은 기존 테마를 덮지 않고 폴더를 하나 더 만든다.** 기존 `japanese`(밤의
  전통 온천 여관)는 그대로 두고 `kabukicho`(네온 장지벽 + 옻칠 바닥의 밤 유흥가)를
  추가했다. 나란히 놓고 비교할 수 있고, 되돌릴 일이 없다.
  등록은 세 곳: `pack-spec.THEMES` / `pub-scene-2d.THEMES`(바닥·벽 색) / `data.themes`.
- 시트 8장을 뽑아 432개를 구웠다. 값을 치른 것 셋:

  **(1) 보라색 옷이 배경으로 지워졌다.** 손님 B의 보라 새틴 드레스가 통째로 사라졌다.
  원인은 `keyMagenta`가 "hue 300±30, 채도 0.35 이상"을 전부 배경으로 본 것 — 드레스는
  hue 280 / 채도 0.7~0.8이라 그 안에 들어간다. 그렇다고 좁게만 자르면 이번엔 예전
  유럽풍 시트처럼 **디더링된 배경**(절반이 채도 0.67)이 남아 12칸이 한 덩어리로 붙는다.
  → 키를 두 단계로 나누고, 임계값을 **시트마다 테두리 2px에서 직접 재서** 정한다.
    1. 배경 본체: 그 시트의 테두리 채도 5백분위를 하한으로 삼아 지운다.
    2. 경계 띠: 이미 지워진 픽셀에 **닿은** 옅은 마젠타만 몇 px 따라 들어간다.
       그림 한가운데 있는 보라 옷은 배경에 닿아 있지 않으므로 살아남는다.
  검증은 테마 6종 × 시트 8장의 **성분 개수**로 한다 — 변경 전후가 같아야 한다.
  (`princess/hall`과 `european/seat`는 이 변경 전부터 어긋나 있던 것으로 확인했다.)
  `tools/_keycheck.mjs` 신설 — 배경과 "지워지면 안 되는 색"이 얼마나 붙어 있는지 잰다.

  **(2) 인물 시트를 다시 뽑을 때는 앞 시트를 레퍼런스로 물린다.** 뒷모습 시트를 글로만
  주문했더니 머리가 흰색으로 튀고(→ "옷보다 밝게"를 극단으로 해석), 비율이 2등신으로
  달라지고, 칸 안에 글자까지 들어왔다. cast 시트를 `image_references`로 넣고 "이 시트의
  비율과 머리색을 그대로"라고 못박으니 한 번에 맞았다.

  **(3) 직원 동작 시트에는 "가구 금지"를 명시한다.** 딜러에게 "테이블에 손을 얹고"라고
  적었더니 포커 테이블이, 바텐더에겐 바 카운터가 스프라이트에 붙어서 그려졌다.

- **서 있는 칸에도 높이 상한을 걸었다**(`ACTOR.stand × 1.15`). 한 시트 한 배율 원칙은
  그대로지만, 시트가 한 인물만 1.6배로 그려 오는 일이 있다. 제대로 그려진 칸은 28px
  근처라 상한에 걸리지 않고 튄 칸만 잡힌다.
- **가부키초 바닥을 밝혔다.** 검은 정장 손님 + 거의 검은 바닥이라 실루엣이 묻혔다.
  분위기는 벽·조명으로 내고 바닥은 실루엣이 읽힐 만큼 값을 띄운다.
- `tools/PROMPTS.md` 신설 후 **실제 `pack-build.mjs`와 대조해 고쳤다** — 인물은 4×6 한 장,
  좌석·직원동작 시트가 따로 있어 테마 하나가 7장이 아니라 8장이다.
- 검증: `node tools/scene2d-preview.mjs 6 0.8 kabukicho` / `... classic` 둘 다 통과,
  클래식 화면은 변경 전과 동일.

**다음 할 일**
- 가부키초 뒷모습 시트의 손님 B가 여전히 다른 사람보다 크게 그려져 있다(상한으로 눌러
  둔 상태). 다시 뽑는다면 cast를 레퍼런스로 물린 채 "모든 인물 같은 키"를 더 강하게.
- 매장 구도가 아직 사각형 한 칸이다. `bar_corner` + `partition`으로 ㄱ자 배치.
- 테이블 사이 여백이 빡빡하다(의자끼리 닿는 구간).

### 2026-09-19 (1차) — 저장소 분리 + 정리

- `holdem-pub-tycoon`의 작업 트리를 복사해 새 저장소 `holdem-pub-iso`로 분리했다.
  깃 이력은 새로 시작한다(기존 `.git` 136MB는 가져오지 않음).
- **아이소메트릭 경로만 남기고 정리**: 3D 렌더러(`scene3d.js`, 142KB)는 두 HTML 모두
  더 이상 참조하지 않는 죽은 코드라 삭제. 구 픽셀 월드 3종과 그에 딸린 preview 도구
  12개, 스크래치 HTML 7개, 구 파이프라인 산출물(`assets/pixel`, `assets/actors`,
  `assets/actors_world` = 114MB)도 함께 정리. 작업 트리 467MB → 4MB.
- 지우기 전에 참조를 전부 확인했다 — `pub-scene-2d.js`가 읽는 건 `assets/pack/` 뿐이고,
  `assets/pixel`·`assets/actors`를 읽던 건 삭제한 `js/pixel/*world*.js` 3종뿐이었다.
- 정리 후 `node tools/scene2d-preview.mjs 6 0.8` 통과 — 인터페이스 6종, 에셋 전부 로드,
  탭 판정 9종 정상.
- `assets/raw/`(생성 원본 279MB)는 gitignore 대상이지만 재굽기가 가능하도록 로컬에는
  복사해 두었다.
- 과거 로그·README는 `docs/`로 옮겼다.

**다음 할 일**
- 새 아트 방향 확정 → 시트 프롬프트를 `tools/PROMPTS.md`에 적고 그 파일을 기준으로 생성.
- 매장 구도가 아직 사각형 한 칸이다. `bar_corner` + `partition`으로 ㄱ자 배치.
- 테이블 사이 여백이 빡빡하다(의자끼리 닿는 구간).
